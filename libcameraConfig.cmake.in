# - Config file for the libcamera package
# It defines the following variables
#  libcamera_INCLUDE_DIRS - include directories for FooBar
#  libcamera_LIBRARIES    - libraries to link against
#  libcamera_EXECUTABLE   - the bar executable

set(libcamera_INCLUDE_DIRS "@CONF_INCLUDE_DIRS@")

# Our library dependencies (contains definitions for IMPORTED targets)
include("${libcamera_DIR}/export_libcamera.cmake")

# These are IMPORTED targets created by libcameraTargets.cmake


